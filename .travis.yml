language: java

jdk:
  - oraclejdk8

env:
  - VERSION=xxx

services:
  - postgresql

cache:
  directories:
  - .autoconf
  - $HOME/.m2

before_script:
  - psql -c 'create database momowars;' -U postgres

script:
  - mvn clean package test